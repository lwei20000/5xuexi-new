<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.struggle.common.system.mapper.DictionaryMapper">

    <!-- 关联查询sql -->
    <sql id="selectSql">
        SELECT a.* FROM sys_dictionary a
        <where>
            AND a.deleted = 0
            <if test="param.dictId != null">
                AND a.dict_id = #{param.dictId}
            </if>
            <if test="param.dictCode != null">
                AND a.dict_code LIKE CONCAT('%', #{param.dictCode}, '%')
            </if>
            <if test="param.dictName != null">
                AND a.dict_name LIKE CONCAT('%', #{param.dictName}, '%')
            </if>
            <if test="param.comments != null">
                AND a.comments LIKE CONCAT('%', #{param.comments}, '%')
            </if>
            <if test="param.createTimeStart != null">
                AND a.create_time &gt;= #{param.createTimeStart}
            </if>
            <if test="param.createTimeEnd != null">
                AND a.create_time &lt;= #{param.createTimeEnd}
            </if>
            <if test="param.keywords != null">
                AND (
                a.dict_code LIKE CONCAT('%', #{param.keywords}, '%')
                OR a.dict_name LIKE CONCAT('%', #{param.keywords}, '%')
                )
            </if>
        </where>
    </sql>

    <!-- 分页查询 -->
    <select id="selectPageRel" resultType="com.struggle.common.system.entity.Dictionary">
        <include refid="selectSql"/>
    </select>

</mapper>
