<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.struggle.common.base.mapper.CoursewareMapper">

    <!-- 关联查询sql -->
    <sql id="selectSql">
        SELECT a.*
        FROM t_courseware a
        <where>
            <if test="param.coursewareId != null">
                AND a.courseware_id = #{param.coursewareId}
            </if>
            <if test="param.courseId != null">
                AND a.course_id = #{param.courseId}
            </if>
            <if test="param.coursewareName != null">
                AND a.courseware_name LIKE CONCAT('%', #{param.coursewareName}, '%')
            </if>
            <if test="param.sortNumber != null">
                AND a.sort_number = #{param.sortNumber}
            </if>
            <if test="param.duration != null">
                AND a.duration = #{param.duration}
            </if>
            <if test="param.size != null">
                AND a.size = #{param.size}
            </if>
            <if test="param.fileType != null">
                AND a.file_type LIKE CONCAT('%', #{param.fileType}, '%')
            </if>
            <if test="param.fileUrl != null">
                AND a.file_url LIKE CONCAT('%', #{param.fileUrl}, '%')
            </if>
            <if test="param.parentId != null">
                AND a.parent_id = #{param.parentId}
            </if>
            <if test="param.deleted != null">
                AND a.deleted = #{param.deleted}
            </if>
            <if test="param.deleted == null">
                AND a.deleted = 0
            </if>
            <!--租户数据权限 -->
            <if test="param.tenantIds != null and param.tenantIds.size()>0">
                AND a.tenant_id in
                <foreach collection="param.tenantIds" open="(" close=")" separator="," item="tenantId">
                    #{tenantId}
                </foreach>
            </if>
            <if test="param.createTimeStart != null">
                AND a.create_time &gt;= #{param.createTimeStart}
            </if>
            <if test="param.createTimeEnd != null">
                AND a.create_time &lt;= #{param.createTimeEnd}
            </if>
        </where>
        order by a.parent_id ,a.sort_number
    </sql>

    <!-- 分页查询 -->
    <select id="selectPageRel" resultType="com.struggle.common.base.entity.Courseware">
        <include refid="selectSql"></include>
    </select>

    <!-- 查询全部 -->
    <select id="selectListRel" resultType="com.struggle.common.base.entity.Courseware">
        <include refid="selectSql"></include>
    </select>

</mapper>
